services:
  fintrack:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8103:8103"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev -- --host 0.0.0.0 --port 8103
